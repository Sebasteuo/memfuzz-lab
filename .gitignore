# === memfuzz-lab: local builds & artifacts (not versioned) ===
out/
build/
targets/*/build/

# Local packages / logs
*.tar.gz
*.zip
*.log

# AFL++ default dirs (just in case someone runs outside `out/`)
default/
findings/
*.bak
