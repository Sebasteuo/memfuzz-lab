name: Inspect workspace          # Nombre del workflow que verás en Actions

on: [workflow_dispatch]          # Solo se ejecuta cuando pulses "Run workflow"

jobs:
  ls:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/sebasteuo/memfuzz-lab:latest
      options: --user 0          # << Arranca el contenedor como root >>
    steps:
      - uses: actions/checkout@v4

      - name: Show first 100 paths
        run: |
          echo "🗂  WORKSPACE = ${{ github.workspace }}"
          ls -R "${{ github.workspace }}" | head -100
