{{ Aquí copia TODO el contenido actual de .github/workflows/fuzz.yml
   pero añade, justo antes del paso "Symbolize crashes", este bloque: }}

      - name: Build target (analyze)
        shell: bash
        run: |
          for target in png_parser qemu_elf; do
            bash ./targets/$target/build.sh
          done

{{ mantén la misma indentación (6 espacios) y, debajo, sigue el paso
   - name: Symbolize crashes … tal como lo tienes }}
